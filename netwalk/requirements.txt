
# Minimal requirements for PyCharm on Windows
# Install in PyCharm: File → Settings → Project → Python Interpreter → +
# Or in PyCharm Terminal: pip install -r requirements_pycharm.txt

# Essential (REQUIRED)
netdev>=0.9.3
textfsm>=1.1.3
ntc-templates>=3.5.0

# Recommended (makes output prettier and adds features)
pandas>=2.0.0
rich>=13.0.0
